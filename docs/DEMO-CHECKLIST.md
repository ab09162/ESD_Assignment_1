Current beginner workflow: open the project-root SCREENSHOT-CHECKLIST.pdf or SCREENSHOT-CHECKLIST.txt. Their prepared links use the fresh 23 September run. The older sequence below is retained as technical reference.

# Remaining manual browser screenshots

All Docker tests/experiments below have already run; rerunning them is not required to capture stored historical charts. Browser rendering was not accessible to automation. Open the real pages, confirm panels render, and capture screenshots yourself. No screenshots have been fabricated.

Set Grafana/Kibana display timezone to **UTC** and use **absolute** ranges on **2026-09-21**, not “last 15 minutes”. The URLs below include Grafana range bounds. Prometheus retains data for 7 days subject to its 1 GB size limit; capture while this data is still retained. If it has expired, rerun the documented scripts and use their new UTC boundaries. Preserve identical axes for phase comparisons.

| Screenshot | Exact page / panels / action |
|---|---|
| 1. API and health | `http://localhost:8000/docs`: expanded room/booking routes and a real response; terminal `docker compose ps -a` for healthy services. Successful exited setup jobs are normal. |
| 2. Prometheus targets | `http://localhost:9090/targets`: campus-app, node-exporter, prometheus, cardinality-demo UP. Demo is optional if later stopped. |
| 3. Metric types | `http://localhost:8000/metrics`: Counter/Gauge/Histogram/Summary TYPE declarations and duration bucket/count/sum samples; no request IDs in application labels. |
| 4. Application overview | [Campus application](http://localhost:3000/d/campus-app?from=1789983303466&to=1789983426535&timezone=utc), 09:35:03–09:37:07 UTC. Capture panels 1–10 (rates, errors, tails, in-flight, routes/statuses), 11–16 (business/DB and **Booking conflict percentage (own exploration)**), and 17–20 (route tail, lead time, gauge health, local log loss). |
| 5. Baseline load/resources | [Campus load](http://localhost:3000/d/campus-load?from=1789983303466&to=1789983426535&timezone=utc), same baseline range: **k6 virtual users**, **Server throughput**, **Server p95 / p99**, **Client p95 / p99**, errors, in-flight, DB and app CPU/RSS. |
| 6. Linux resource scope | [Campus node](http://localhost:3000/d/campus-node?from=1789983303466&to=1789983426535&timezone=utc), same baseline: all six panels, including device/mountpoint legends. Caption: Docker Desktop Linux/WSL2 scope; network namespace and shared-mount limitations, not native Windows monitoring. |
| 7. Correlated indexed request | Kibana `http://localhost:5601/app/discover`, data view `campus-logs-*`, **09:22:45–09:23:10 UTC**, KQL `request_id : "verify-eb604ba0-b5a9-49fb-b080-f2c46ad17542"`. Add `method,route,status_code,duration_ms,severity,request_id`; expand the actual 201 document (12.034 ms). |
| 8. Baseline phase | [Campus load](http://localhost:3000/d/campus-load?from=1790004751725&to=1790004874224&timezone=utc), **15:32:31.725–15:34:34.224 UTC**. Capture VUs, server/client tails, throughput, errors, in-flight, DB mean, VM CPU/memory and **Injected delays per second** (panel 14). Use the report's k6 summary for exact client run percentiles. |
| 9. Fault phase | [Campus load](http://localhost:3000/d/campus-load?from=1790005188301&to=1790005312915&timezone=utc), **15:39:48.301–15:41:52.915 UTC**. Capture VUs, server/client tails, throughput, errors, in-flight, DB mean, VM CPU/memory and **Injected delays per second** (panel 14). Use the report's k6 summary for exact client run percentiles. |
| 10. Recovery phase | [Campus load](http://localhost:3000/d/campus-load?from=1790005627301&to=1790005750557&timezone=utc), **15:47:07.301–15:49:10.557 UTC**. Capture VUs, server/client tails, throughput, errors, in-flight, DB mean, VM CPU/memory and **Injected delays per second** (panel 14). Use the report's k6 summary for exact client run percentiles. |
| 11. Fault causality | Kibana Discover, **15:39:48.301–15:41:52.915 UTC**. First KQL `message : "fault_delay_injected"`; then `request_id : "e6b72c73-7092-4008-9ad4-f019f6500802"`. Show the 500 ms warning and matching 508.860 ms completed request together. |
| 12. Recovery absence of new faults | Kibana Discover, **15:47:07.301–15:49:10.557 UTC**, KQL `message : "fault_delay_injected"` returns no hits. Pair with recovery latency chart; older warnings outside this interval do not indicate a still-enabled fault. |
| 13. Increasing users/capacity | [Campus load](http://localhost:3000/d/campus-load?from=1789983465296&to=1789984247461&timezone=utc), **09:37:45–09:50:48 UTC**. Show 10→25→50→100→200 VUs alongside throughput, tails, 5xx, in-flight, app CPU and DB mean. Caption: saturation beyond the observed 50-user plateau; 271 unexpected business responses, controlled pool-timeout 503s at high load. Overall thresholds passing does not mean every stage was healthy. |
| 14. Cardinality 100→1 | Prometheus `http://localhost:9090/graph`, graph `count(demo_requests_total{job="cardinality-demo"})` over **15:49:10–15:49:59 UTC**. Show high 100 and low 1; compare `sum(demo_requests_total{job="cardinality-demo"})` (100 in each settled mode). Widen range if needed to show historical high samples. |
| 15. Persisted concurrency/retention evidence | Attach `docs/evidence/docker/pytest.txt`, `concurrency.json` (1 winner/19 conflicts), and `stack-verification.json` (ILM managed/policy). A terminal capture of these actual files is acceptable; do not claim seven-day deletion was observed. |

For a single anomaly overview use [this combined range](http://localhost:3000/d/campus-load?from=1790004751725&to=1790005750557&timezone=utc). Quiet gaps are deliberate 310-second cooldowns. Empty live k6 panels after a run ends are expected because stale markers are sent; historical ranges preserve the measurements.

No runtime setup remains except keeping Docker running. Uploading the local commit to your chosen GitHub repository and adding real screenshots to your submission remain manual. See [GitHub audit](GITHUB-READINESS.md).
